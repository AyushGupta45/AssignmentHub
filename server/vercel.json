{
  "version": 2,
  "buildCommand": "cd ../client && npm install && npm run build",
  "functions": {
    "index.js": {
      "includeFiles": "public/**"
    }
  },
  "routes": [
    { "src": "/(.*)", "dest": "index.js" }
  ]
}
